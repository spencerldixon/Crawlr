<nav class="navbar navbar-default navbar-fixed-top navbar-inverse" role="navigation">
  <div class="container"<!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <a class="navbar-brand" href="#">Fendix Crawler</a>
  </div>

  <ul class="nav navbar-nav">
    <% if user_signed_in? %>
      <li><%= link_to "Dashboard", dashboard_index_path %></li>

      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Sites <b class="caret"></b></a>
        <ul class="dropdown-menu">
          <li><%= link_to "All Sites", sites_path %></li>
          <li><%= link_to "New Site", new_site_path %></li>
        </ul>
      </li>
    <% end %>
    <% if user_signed_in? && current_user.admin? %>
      
        <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin <b class="caret"></b></a>
        <ul class="dropdown-menu">
          <li><%= link_to "All Users", admin_index_path %></li>
        </ul>
      </li>
    <% end %>
  </ul>

  <ul class="nav navbar-nav navbar-right">
    <% if user_signed_in? %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">My Account<b class="caret"></b></a>
        <ul class="dropdown-menu">
          <li><%= link_to "Edit my Account", edit_user_registration_path %></li>
          <li><%= link_to "Log Out", destroy_user_session_path, method: "delete" %></li>
        </ul>
      </li>
    <% else %>
      <li><%= link_to "Sign Up", new_user_registration_path %></li>
      <li><a href="mailto:spencerlloyddixon@gmail.com">Contact</a></li>
    <% end %>
  </ul>
</div>
</nav>